
  # Create design from images

  This is a code bundle for Create design from images. The original project is available at https://www.figma.com/design/WAU59QEtnPdAK5vpCWFN9a/Create-design-from-images.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  